CREATE EXTERNAL TABLE test_db.test_table (
`my_int` BIGINT COMMENT `This is an integer`
`my_double` DOUBLE COMMENT ``
`my_date` DATE COMMENT ``
`my_decimal` DECIMAL(10,2) COMMENT ``
)
PARTITION BY (
`my_timestamp` TIMESTAMP COMMENT `Partition column`
)
ROW FORMAT SERDE
    'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
    'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
    'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
    's3://bucket/test_table'
TBLPROPERTIES (
    'classification'='parquet',
    'parquet.compression'='SNAPPY'
);